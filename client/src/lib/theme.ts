import type { Theme } from "@emotion/react";

const colors: Record<string, React.CSSProperties["color"]> = {
	// TODO: this is just here see if the theme works
	primary: "royalblue"
};

export const theme: Theme = {
	colors
};
